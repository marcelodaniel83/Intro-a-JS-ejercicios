/*Ejercicio 2: Un bibliotecario desea un simple programa en el que ingrese el nombre y el apellido de alguien por separado y
lo muestre normalizado. Normalizado significa que debería verse así: Apellido, Nombre. */

var apellido;
var nombre;

apellido = prompt('Ingrese apellio: ');
nombre = prompt('Ingrese nombre ');
alert('El nombre completo es ' +apellido +" " +nombre);
